#!/bin/sh
# Please install these libraries for the program to return
# Also this program runs on python 2.7. Anything else would give errors.
# The commands below assume you have *nix environment. I am not aware of the the commands for Windows.
pip install cryptography
pip install requests
pip install simplejson
